// PLACEHOLDER — Architecture prepared. Implementation required.
// MetricCard — displays single KPI: large number + comparison to previous period. Used when row=1, col=1 or output_format=metric_card.

export default function PlaceholderPage() { return null; }

