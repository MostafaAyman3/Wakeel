// PLACEHOLDER — Architecture prepared. Implementation required.
// CustomerInputForm — customer-facing form: identifier type selector (order_id | invoice_id | customer_id) + identifier value input + issue description textarea + submit button.

export default function PlaceholderPage() { return null; }

