# PLACEHOLDER — Architecture prepared. Implementation required.

"RAG Service — manages tax rule document loading, chunking, embedding (text-embedding-3-small), and pgvector storage. Used by tax_rag_tool. Also provides retrieval interface for the tool."

