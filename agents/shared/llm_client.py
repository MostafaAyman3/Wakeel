# PLACEHOLDER — Architecture prepared. Implementation required.

"Shared LLM Client — single instance of GPT-4o and GPT-4o-mini. Import from here in all M1 and M3 nodes. Never instantiate ChatOpenAI directly in node files. Also initializes text-embedding-3-small for Tax RAG."

